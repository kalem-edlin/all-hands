name: reviewer
flow: CODE_REVIEW.md
message_template: |
  Reviewing prompt: ${PROMPT_PATH}
  Changes since: ${COMMIT_REF}
  Alignment doc: ${ALIGNMENT_PATH}
template_vars:
  - PROMPT_PATH
  - COMMIT_REF
  - ALIGNMENT_PATH
