name: compounder
flow: COMPOUNDING.md
prompt_scoped: false
message_template: |
  Milestone spec path: ${SPEC_PATH}
  Alignment doc path: ${ALIGNMENT_PATH}
  Prompt files path: ${PROMPTS_FOLDER}
template_vars:
  - SPEC_PATH
  - ALIGNMENT_PATH
  - PROMPTS_FOLDER
